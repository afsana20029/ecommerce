import 'package:flutter/material.dart';

class CenterCircularProgressIndicater extends StatelessWidget {
  const CenterCircularProgressIndicater({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
